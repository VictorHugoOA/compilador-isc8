program {
     int x, y;
     float a, b;
     bool c, d;
     c = true;
     x=5; // comentario 1
     y=4; /* comentario 2 */
     a=0.0;
     b=3.0;
     do {
          if(x<y and y>=0) then{
               y=y-1;
          } else {
               x=x-2;
               a=a*x+b;
               y=y-1;
          } 
fi
while(a==3){ 
write a;
          }
     } until(c);
}